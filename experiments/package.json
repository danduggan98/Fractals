{
    "dependencies": {
        "webpack": "4.44.1",
        "webpack-cli": "3.3.12",
        "wasm-server": "0.0.4"
    },
    "scripts": {
        "refresh": "cargo build && wasm-pack build --dev && node_modules/.bin/webpack && cd dist/ && ../node_modules/.bin/wasm-server"
    }
}
